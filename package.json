{
  "name": "protractor-test",
  "version": "1.0.0",
  "description": "protractor test sample",
  "main": "index.js",
  "scripts": {
    "start": "protractor conf.js",
    "start-selenium-server": "webdriver-manager start",
    "update": "webdriver-manager update",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jasmine-spec-reporter": "^5.0.2"
  }
}
